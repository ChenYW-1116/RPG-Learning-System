# 模組提取報告

## 📋 差異分析摘要

### 比對版本
- **Base_Code**: `雅思寫作衝刺教練.html` (1440 行, 功能完整但無 AI 功能)
- **Target_Code**: `雅思寫作衝刺教練v1.html` (526 行, 輕量化 + AI 增強版)

### 核心架構差異

| 維度 | Base_Code | Target_Code |
|------|-----------|-------------|
| **代碼量** | 1440 行 | 526 行 |
| **架構模式** | 重量級 MVC (多 Class) | 輕量級單 Class |
| **AI 功能** | ❌ 無 | ✅ Gemini API 整合 |
| **API 調用** | 模擬 (Mock) | 真實 API 調用 |
| **重試機制** | 無 | 指數退避重試 |
| **UI 複雜度** | 完整儀表板 + 圖表 | 精簡三分頁 |

---

## 🧩 提取的功能模組

### 1. gemini-api-wrapper (核心基礎設施)
**提取原因**: Target_Code 中新增了完整的 Gemini API 調用邏輯，包含:
- 指數退避重試機制 (`retryDelays = [1000, 2000, 4000]`)
- JSON/文本雙模式響應解析
- 統一的錯誤處理

**掛載模式**: `Reference` - 封裝為獨立 Class

**Target Slot**: `@slot:api_service_layer`

**獨立性評估**: ⭐⭐⭐⭐⭐ (完全獨立，無業務邏輯耦合)

---

### 2. ai-essay-analyzer (業務功能)
**提取原因**: Target_Code 新增 AI 範文分析功能:
- 識別被動句 (Grammar Range)
- 提取高級詞彙 (Lexical Resource)
- 預估雅思分數

**掛載模式**: `Reference` - 封裝為業務邏輯函數

**Target Slot**: `@slot:feature_essay_analysis`

**獨立性評估**: ⭐⭐⭐⭐ (依賴 gemini-api-wrapper)

---

### 3. ai-essay-rewriter (業務功能)
**提取原因**: Target_Code 新增 AI 高階改寫功能:
- 將普通文章升級為 Band 9 水準
- 聚焦詞彙和語法提升

**掛載模式**: `Reference` - 封裝為業務邏輯函數

**Target Slot**: `@slot:feature_essay_rewrite`

**獨立性評估**: ⭐⭐⭐⭐ (依賴 gemini-api-wrapper)

---

### 4. ai-inspiration-generator (業務功能)
**提取原因**: Target_Code 新增 AI 靈感助手:
- 根據議題生成正反觀點
- 推薦學術詞彙

**掛載模式**: `Reference` - 封裝為業務邏輯函數

**Target Slot**: `@slot:feature_inspiration`

**獨立性評估**: ⭐⭐⭐⭐ (依賴 gemini-api-wrapper)

---

### 5. ai-blind-write-diagnosis (業務功能)
**提取原因**: Target_Code 在盲寫流程中新增 AI 診斷:
- 比對原文與盲寫嘗試
- 指出關鍵語法/詞彙錯誤
- 提供鼓勵性反饋

**掛載模式**: `Snippet` - 必須嵌入盲寫評估主流程

**Target Slot**: `@slot:blind_write_diagnosis`

**獨立性評估**: ⭐⭐⭐ (依賴 gemini-api-wrapper + 盲寫狀態)

---

### 6. ui-loader-manager (UI 工具)
**提取原因**: Target_Code 統一了加載狀態管理模式:
- 按鈕禁用/啟用
- 加載動畫顯示/隱藏

**掛載模式**: `Snippet` - 輕量級工具函數

**Target Slot**: `@slot:ui_utilities`

**獨立性評估**: ⭐⭐⭐⭐⭐ (完全獨立)

---

## 📁 文件結構

```
.agent/
└── skills/
    ├── MODULE_EXTRACTION_REPORT.md     # 本報告
    ├── gemini-api-wrapper/
    │   └── SKILL.md                    # API 封裝技能
    ├── ai-essay-analyzer/
    │   └── SKILL.md                    # 範文分析技能
    ├── ai-essay-rewriter/
    │   └── SKILL.md                    # 高階改寫技能
    ├── ai-inspiration-generator/
    │   └── SKILL.md                    # 靈感助手技能
    ├── ai-blind-write-diagnosis/
    │   └── SKILL.md                    # 盲寫診斷技能
    └── ui-loader-manager/
        └── SKILL.md                    # 加載狀態管理技能
```

---

## 🔗 依賴關係圖

```
                    ┌─────────────────────────┐
                    │   gemini-api-wrapper    │
                    │   (核心基礎設施層)       │
                    └───────────┬─────────────┘
                                │
        ┌───────────┬───────────┼───────────┬───────────┐
        ▼           ▼           ▼           ▼           ▼
┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌──────────────┐
│  essay    │ │  essay    │ │inspiration│ │blind-write│ │ ui-loader    │
│ analyzer  │ │ rewriter  │ │ generator │ │ diagnosis │ │ manager      │
└───────────┘ └───────────┘ └───────────┘ └───────────┘ └──────────────┘
    業務層        業務層        業務層        業務層         工具層
```

---

## ⚙️ Slot 掛載點總覽

| Slot 名稱 | 掛載位置 | 模組 | 掛載模式 |
|-----------|----------|------|----------|
| `@slot:api_service_layer` | 主程式頂部 (初始化區) | gemini-api-wrapper | Reference |
| `@slot:ui_utilities` | 工具函數區 | ui-loader-manager | Snippet |
| `@slot:feature_essay_analysis` | 精讀分析分頁 | ai-essay-analyzer | Reference |
| `@slot:feature_essay_rewrite` | 精讀分析分頁 | ai-essay-rewriter | Reference |
| `@slot:feature_inspiration` | 數據看板分頁 | ai-inspiration-generator | Reference |
| `@slot:blind_write_diagnosis` | 盲寫評估函數尾部 | ai-blind-write-diagnosis | Snippet |

---

## 📝 Agent 執行指南

### 當 Agent 讀取 skills.md 時的掛載邏輯:

1. **Reference 模式處理**:
   - 在主程式頂部補上 `import { Module } from './path'` 或內聯 Class 定義
   - 在對應 `@slot` 處生成 `const instance = new Module(config)`
   - 綁定事件監聽器

2. **Snippet 模式處理**:
   - 讀取 `Implementation Reference` 中的代碼片段
   - 根據當前上下文變量名進行微調
   - 直接填充到 `@slot` 標記位置

3. **依賴解析**:
   - 優先掛載 `gemini-api-wrapper` (無依賴)
   - 優先掛載 `ui-loader-manager` (無依賴)
   - 再按需掛載業務功能模組

---

## ✅ 驗證清單

- [ ] 所有 Skill 文件包含必要欄位 (Name, Mode, Slot, Contract, Reference)
- [ ] 依賴關係正確聲明
- [ ] Glue Code 可直接複製使用
- [ ] UI 模板與功能邏輯分離
- [ ] 錯誤處理邏輯完整
